  - name: Uptime Action

  uses: srt32/uptime@v0.2.1

on:

  schedule:

    - cron: '*/2 * * * *'

jobs:

  ping_site:

    runs-on: ubuntu-latest

    name: Ping the site

    steps:

    - name: Check the site

      id: hello

      uses: srt32/uptime@master

      with:

        url-to-hit: "https://KellyWorld.kettraworld.repl.co"

        expected-statuses: "200,301"
