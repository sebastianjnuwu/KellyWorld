generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model guild {
  id       String   @id @map("_id")
  language String   @default("pt-BR")
}